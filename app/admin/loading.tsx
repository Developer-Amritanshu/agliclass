import { PageLoading } from "@/components/ui/page-loading";

export default function Loading() {
  return <PageLoading title="Loading operations" body="Fetching queues, assignments, and live admin data." />;
}
